{
  "name": "pdf_reader",
  "version": "1.0.0",
  "description": "pdf reading app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "npm install && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
    "heroku-postbuild": "npm run build"
  },
  "keywords": [],
  "author": "tik_22",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}
